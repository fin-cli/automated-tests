# behat.yml
default:
  suites:
    # FP-CLI bundle
    fp-cli/fp-cli-bundle:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/fp-cli-bundle/features

    # FP-CLI framework
    fp-cli/fp-cli:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/fp-cli/features

    # fp cache
    fp-cli/cache-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/cache-command/features

    # fp checksum
    fp-cli/checksum-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/checksum-command/features

    # fp config
    fp-cli/config-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/config-command/features

    # fp core
    fp-cli/core-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/core-command/features

    # fp cron
    fp-cli/cron-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/cron-command/features

    # fp db
    fp-cli/db-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/db-command/features

    # fp embed
    fp-cli/embed-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/embed-command/features

    # fp comment | fp menu | fp network | fp option | fp post | fp site | fp taxonomy | fp term | fp user
    fp-cli/entity-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/entity-command/features

    # fp eval
    fp-cli/eval-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/eval-command/features

    # fp export
    fp-cli/export-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/export-command/features

    # fp plugin | fp theme
    fp-cli/extension-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/extension-command/features

    # fp i18n
    fp-cli/i18n-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/i18n-command/features

    # fp import
    fp-cli/import-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/import-command/features

    # fp language
    fp-cli/language-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/language-command/features

    # fp maintenance-mode
    fp-cli/maintenance-mode-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/maintenance-mode-command/features

    # fp media
    fp-cli/media-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/media-command/features

    # fp package
    fp-cli/package-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/package-command/features

    # fp rewrite
    fp-cli/rewrite-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/rewrite-command/features

    # fp role
    fp-cli/role-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/role-command/features

    # fp scaffold
    fp-cli/scaffold-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/scaffold-command/features

    # fp search-replace
    fp-cli/search-replace-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/search-replace-command/features

    # fp server
    fp-cli/server-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/server-command/features

    # fp shell
    fp-cli/shell-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/shell-command/features

    # fp super-admin
    fp-cli/super-admin-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/super-admin-command/features

    # fp widget
    fp-cli/widget-command:
      contexts:
        - FP_CLI\Tests\Context\FeatureContext
      paths:
        - vendor/fp-cli/widget-command/features
